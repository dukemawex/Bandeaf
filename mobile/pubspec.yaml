name: family_connect
version: 0.1.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.4.3+1
  flutter_blue_plus: ^1.31.18
  flutter_map: ^7.0.2
  flutter_map_tile_caching: ^9.0.1
  geolocator: ^11.0.0
  flutter_secure_storage: ^9.2.2
  background_fetch: ^1.3.3
  geofence_service: ^6.0.0+2
  http: ^1.2.1
  local_notifications: ^0.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
